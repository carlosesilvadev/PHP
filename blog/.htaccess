#Ocultar exibição de diretórios com sinal de menos
Options -indexes

#URL Amigavel
RewriteEngine On
RewriteBase /blog

RewriteCond %{SCRIPT_NAME} !-f
RewriteCond %{SCRIPT_NAME} !-d
RewriteCond %{SCRIPT_NAME} !-l

RewriteRule ^(.*)$ index.php/$1

